A plugin marketplace of agent skills for _Claude_ and _Codex_.

**Claude Code** — add the marketplace, then install a skill: `/plugin marketplace add pymmog/skills` followed by `/plugin install <skill>@skills`. **Codex CLI** — add the plugin from the repo: `codex plugin add https://github.com/pymmog/skills.git` (skills under `skills/` are auto-discovered).
